/* eslint-disable */
const state ={
    todo: [],
    todo: null,
    todoLoading: false,
       
    };
    const getters ={
        user: (state) => state.user,
    };
    const actions ={
        
    };
    const mutations ={};
    export default {
        state, getters, actions,mutations, namespaced: true,
    };